{
  "name": "lingobridge",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "build:extension:release": "node scripts/build-extension-release.mjs",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test",
    "dev:dashboard": "pnpm build:packages && pnpm --filter @lingobridge/dashboard dev",
    "dev:extension": "pnpm --filter @lingobridge/extension dev",
    "dev:gateway": "pnpm --filter @lingobridge/gateway dev",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "icons": "node scripts/generate-icons.mjs",
    "lint": "biome lint .",
    "test": "pnpm build:packages && pnpm --filter @lingobridge/tests test",
    "typecheck": "pnpm build:packages && pnpm -r --if-present typecheck",
    "build:packages": "pnpm --filter @lingobridge/contracts build && pnpm --filter @lingobridge/database build && pnpm --filter @lingobridge/auth build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "sharp": "0.35.4",
    "typescript": "5.9.3"
  }
}
